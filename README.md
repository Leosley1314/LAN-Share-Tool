# 局域网共享工具 (LAN Share Tool)

Windows 一键配置局域网共享，支持磁盘/文件夹/打印机共享。

## 功能
- 图形界面（GUI），无需命令行
- 选择磁盘或自定义文件夹共享
- 一键共享所有非 C 盘磁盘
- 取消所有共享（关闭 SMB1 + 停止 Server 服务）
- 无密码访客访问 / 指定用户名密码
- 网络发现开关
- 只读 / 读写权限
- 打印机共享开关
- 查看当前共享状态
- 一键重启资源管理器
- 选项悬停提示
- 免责协议确认
- 完成后显示 Windows / 手机访问地址
- 自动启用 SMB1（注册表 + DISM）
- 自动配置防火墙（专用/公用网络均放行）
- 自动启动所需系统服务

## 兼容性
- Windows Vista / 7 / 8 / 8.1 / 10 / 11
- PowerShell 2.0+

## 使用
双击 `局域网共享工具.exe`，选择共享内容后点"开始配置"。

## 客户端设置（访问方）

如果其他 Windows 电脑无法访问，提示 0x80070035 或"找不到网络路径"，请在**访问方电脑**上执行：

1. **启用不安全的来宾登录**
   - Win+R → `gpedit.msc` → 计算机配置 → 管理模板 → 网络 → Lanman 工作站 → 启用"不安全的来宾登录"
   - 或注册表：`HKLM\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation` 新建 DWORD `AllowInsecureGuestAuth` = 1

2. **安装 SMB 1.0/CIFS 客户端**
   - 控制面板 → 程序 → 启用或关闭 Windows 功能 → 勾选"SMB 1.0/CIFS 文件共享支持"

3. **优先用 IP 访问**
   - 不要用电脑名，直接输入 `\\对方IP`（如 `\\192.168.1.100`）

## 下载
[Releases](../../releases)

# 免责声明 (Disclaimer)

使用本工具即表示你同意以下条款：

1. 本工具用于在局域网内共享文件和打印机，请仅在可信网络中使用。
2. 无密码访客(Guest)模式下，同一网络内任何设备均可访问共享内容，请勿在公共网络环境开启。
3. 共享整个磁盘会暴露该盘全部文件，可能被误删、篡改或泄露，请先确认无敏感数据。
4. 请仅在你信任并可控的网络中使用；配置完成后建议及时关闭不再需要的共享。
5. 本工具按原样提供，不附带任何担保；因使用导致的数据损失由使用者自行承担。

## 感谢豆包AI对我的项目做出的帮助
